# CI test dependencies (lightweight subset of production deps)
# Full dependency list and version pins are managed via pyproject.toml + uv.lock
# To regenerate requirements.txt: uv export --no-dev --no-hashes --format requirements-txt -o requirements.txt

# Test runner
pytest
pytest-asyncio

# Runtime deps required by CI-targeted tests
twitchio
requests
aiohttp
obsws-python
python-dotenv
tenacity
psutil
